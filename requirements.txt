# Runtime dependencies.
# Hand-written and deliberately loose: this project needs no pinned build strings.
# CPU-only torch is sufficient — see README for the install note.
numpy>=1.24
pandas>=2.0
torch>=2.0
yfinance>=0.2.40
matplotlib>=3.7

# Live scoreboard: free news via RSS, local finance-tuned sentiment.
# transformers pulls FinBERT weights on first use and caches them; no API key, no quota.
feedparser>=6.0
transformers>=4.40

# Development
pytest>=7.4
ruff>=0.5
